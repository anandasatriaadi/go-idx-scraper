{
  "name": "go-idx-scraper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix idx-web run dev",
    "build": "npm --prefix idx-web run build",
    "preview": "npm --prefix idx-web run preview"
  }
}
